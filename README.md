# InstagramLikeApp (React Native)

##### Start React Native application

Install and launch RN app with those commands:

```
yarn
pod install --project-directory=ios
npx react-native run-ios
```

##### Run tests

```
yarn test --coverage
```
