import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  description: {
    fontSize: 16,
    margin: 20,
  },
  photo: {
    height: 250,
    width: '100%',
  },
});

export default styles;
