import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  flatList: {
    minHeight: '50%',
  },
  emptyContainer: {
    flex: 1,
    padding: 30,
  },
  emptyText: {
    alignSelf: 'center',
  },
});

export default styles;
